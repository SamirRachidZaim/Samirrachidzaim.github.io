name: Disabled - Update Google Scholar counts

# This workflow has been intentionally disabled to stop automatic fetching of Google Scholar counts.
# Automatic scraping from CI runners was unreliable and was blocked by Google. The repository now uses manual updates of `assets/scholar.json`.

on: []  # no triggers

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow has been disabled. See docs/SCHOLAR_SETUP.md for manual update instructions."